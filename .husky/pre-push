#!/bin/sh
echo ""
echo "🔍 Pre-push: TypeScript check..."
npx tsc --noEmit
TSC=$?
if [ $TSC -ne 0 ]; then
  echo "❌ TypeScript failed — push blocked. Fix errors above."
  echo "   Bypass: git push --no-verify"
  exit 1
fi
echo "✅ TypeScript clean — pushing"
